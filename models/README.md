# Model artifacts

ML engineers place trained artifacts here, for example:

- `quality_sulfur.joblib`
- `reliability_proxy.joblib`

Wire them through `QualityAgentML` / `ReliabilityAgentML` without changing `Orchestrator`.
