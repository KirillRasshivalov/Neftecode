# Кириллу → Роману — ответ на сводку 19.09 (дата ответа: 20.09.2026)

Кирилл. Ветка `master` спулена (merge `neuezeldaa/agent-work` уже на origin). Ниже — что закрыто с моей стороны по твоей доке и что нужно от тебя дальше.

---

## 1. Что сделано (ответы на твои вопросы из §1 / §6 / §8)

| Вопрос / задача | Решение | Где |
|---|---|---|
| `pyarrow`, `streamlit`, `jupyter`, `scipy` | Добавлены в `requirements.txt` и `pyproject.toml` (`scipy`/`pyarrow` в основные deps; `jupyter`/`streamlit` — optional `notebook` + в requirements для чистого клона) | `requirements.txt`, `pyproject.toml` |
| `*.md` не попадали в git | Убран ignore `*.md`. Плюс разрешены `models/*.json` (коэффициенты без sklearn в боевом пути) | `.gitignore` |
| `lims_max_age_minutes` 1440 → 1800 | **Подтверждаю.** Оставлено 1800. Комментарий в конфиге обновлён | `configs/constraints.yaml` |
| Gate: опора на ПАК при устаревшем ЛИМС | **Сделал я.** Если ЛИМС старше порога (или отсутствует), но `data_flags.pak_sulfur.healthy == true` — цикл **не отказывается**, пишется warning. Отказ только когда нет ни свежей лаборатории, ни исправного анализатора | `src/neftecode/safety/data_quality_gate.py` |
| Сообщения gate по-русски в часах | Да. Пример: «анализ ЛИМС 348 ч назад при допустимых 30 ч; поточный анализатор недоступен или неисправен» | тот же файл + `tests/test_data_quality_gate.py` |
| Hard check по **p95**, не по среднему | **Сделал я.** Берётся `QualityAssessment.interval_for("sulfur_mg_kg").p95`; если интервала нет (stub) — fallback на среднее | `src/neftecode/safety/constraints.py` |
| E3 — результат проверки с запасом | `ConstraintResult(ok, reasons, margins)`; unpacking `ok, reasons = ...` сохранён. Optimizer пишет `constraint_margins` в сценарий | `constraints.py`, `optimizer.py` |
| E1 — типизированный интервал | `MetricInterval` + поле `intervals` у `QualityAssessment`; baseline заполняет и typed, и `details` (обратная совместимость) | `domain/agent_results.py`, `agents/quality.py` |
| E2 — поле `outcome` | `OperatorRecommendation.outcome ∈ {recommend, hold, refuse}`; оркестратор выставляет на всех путях | `domain/recommendation.py`, `orchestration/orchestrator.py` |
| E4 — окна истории в `ProcessState` | Поле `feature_windows` + `windows_for_agents()` (читает typed или старый `data_flags`). Мост `run_real` заполняет оба | `domain/state.py`, `scripts/run_real.py`, `agents/quality.py` |
| `ASSUMPTIONS.md` в сдачу | Скопирован в корень репо, пометка про gitignore снята, строка про 1800/ПАК обновлена | `ASSUMPTIONS.md` |
| Проверки смеси из blending → `safety/` | **Не переносил.** Ты писал «если хочешь» — оставил у тебя в `agents/blending.py`, чтобы не ломать демо-недели без нужды. Можем перенести отдельным шагом | — |

Док для контекста лежит у нас в репо: `docs/ДЛЯ_КИРИЛЛА.md`.

---

## 2. Проверка

- `pytest`: **108 passed**
- `python -m neftecode run --scenario stale_data` — отказ с русской формулировкой в часах и `outcome: "refuse"`
- `python -m neftecode run --scenario normal` — без регрессии по CLI

Изменения **ещё не закоммичены** в git (ждут явного «коммить»). Список файлов — в конце.

---

## 3. Что делать тебе дальше (приоритет)

### Срочно (до сдачи / ноутбука 22.09)

1. **Закоммить + запушь** (или дождись моего коммита — напиши). Без этого `ASSUMPTIONS.md` / `README.md` снова не уедут на защиту, даже после правки `.gitignore`.
2. **Прогони демо-недели на реальных данных** после моего gate/p95 — особенно `degraded` и `shutdown`:
   ```bash
   python -m scripts.run_real --week stable
   python -m scripts.run_real --week risk
   python -m scripts.run_real --week degraded
   python -m scripts.run_real --week shutdown
   ```
   Ожидание: меньше ложных REFUSE из‑за «дыры» 11:00–14:00 при живом ПАК; при мёртвых ЛИМС+ПАК — по-прежнему REFUSE. Если доли HOLD/RECOMMEND/REFUSE уехали — сверни в таблицу и кинь.
3. **Ноутбук** `notebooks/neftecode_pipeline.ipynb` — прогон с чистого клона + трёх сырых файлов (как ты писал ~1.5 мин). Убедись, что `pyarrow`/`jupyter` подтягиваются из обновлённого `requirements.txt`.
4. **Артефакты моделей в git:** `models/*.json` теперь не игнорируются. Если `breach_classifier.json`, `blend_components.json` и baseline-параметры нужны для воспроизводимости без переобучения — **положи и закоммить** их (или явно опиши в README шаг генерации из `scripts/analysis/*`).

### Желательно до защиты

5. **Интерфейс / карточка** (если ты или фронт её трогаете) — правила из твоего §8:
   - `audit.economy` — информация, не кнопка действия;
   - лимит серы режима подписывать как бюджет смеси, не «мы ослабили ГОСТ»;
   - катализатор `start_up` / `too_short` — «не оценивается», не нули;
   - ПАК не ставить рядом с прогнозом серы как «второй анализ».
6. **Ключи демо-недель** в UI: `stable` / `risk` / `degraded` / `shutdown` (не старые три).
7. На защите проговорить вслух: **10 мг/кг — норма товарной смеси**; гидроочищенный ДТ проверяется по серному бюджету от агента смеси (часто ~11.4).

### Опционально (когда будет час)

8. Перенос жёстких проверок смеси из `agents/blending.py` в `safety/constraints.py` (пять проверок: сера, T95, цетан, плотность, доли/запасы) — чтобы соблюсти CLAUDE.md §9 «все hard в одном месте». Могу сделать я в следующем заходе.
9. Совместная оптимизация режим + смесь — только если останется время; последовательная схема уже ок для демо.

---

## 4. Контракт, на который можно опираться дальше

```text
ProcessState.feature_windows          # E4
QualityAssessment.intervals[metric]   # E1  (p05/mean/p95)
HardConstraints → ConstraintResult    # E3  (.ok / .reasons / .margins)
OperatorRecommendation.outcome        # E2  recommend | hold | refuse
```

Gate больше не должен отказывать на здоровом часовом темпе только из‑за суточного ЛИМС, если ПАК исправен.

---

## 5. Файлы в этом ответе (uncommitted)

```text
.gitignore
requirements.txt
pyproject.toml
ASSUMPTIONS.md
configs/constraints.yaml
configs/assumptions.md
docs/ДЛЯ_КИРИЛЛА.md
src/neftecode/domain/{state,agent_results,recommendation,__init__}.py
src/neftecode/safety/{constraints,data_quality_gate,__init__}.py
src/neftecode/agents/{optimizer,quality}.py
src/neftecode/orchestration/orchestrator.py
scripts/run_real.py
tests/test_constraints.py
tests/test_data_quality_gate.py
tests/test_orchestrator_stub.py
```

Плюс снова отслеживаются локальные `README.md` / `PROJECT_GUIDE.md` / `models/README.md` после снятия `*.md` ignore — их тоже стоит включить в коммит сдачи.

---

## 6. Короткий вердикт тебе

Блокеры из твоего §1 и открытый хвост §8 по `safety/` + domain — **закрыты**. Дальше критичный путь на тебе: **репрогон недель + ноутбук + JSON-модели в репо + коммит/пуш**. Если что-то поедет на `degraded` после PAK-fallback — пиши, поправим порог или формулировки вместе.
